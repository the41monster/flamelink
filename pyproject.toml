[project]
name = "flamelink"
version = "0.1.0"
requires-python = ">=3.14"

dependencies = [
    "fastapi",
    "httpx>=0.28.1",
    "uvicorn",
]

[tool.setuptools.packages.find]
where = ["."]
include = []
